# GenAI Operations Dependencies - Trail Guide Agent
# Adventure Works Outdoor Gear - AI Trail Assistant
# Updated: 2026-08-20
#
# Windows ARM64
#   Use CPython 3.11 ARM64 (matches GitHub Actions). Default `python` on this
#   machine is 3.14 ARM64 — many packages still lack cp314-win_arm64 wheels.
#     py -3.11-arm64 -m venv .venv
#     .venv\Scripts\Activate.ps1
#     python -m pip install --upgrade pip
#     python -m pip install -r requirements.txt
#   cryptography 47+ dropped official win_arm64 wheels (latest would compile
#   from source and needs Rust). Pin 46.0.3 on Windows ARM64 only.
#   PyYAML has no cp311-win_arm64 wheel; lab scripts do not import yaml.

# Core Azure AI Projects SDK (pinned to the API used by src/)
azure-ai-projects==2.0.0b4
azure-identity==1.25.3
azure-core==1.38.2
azure-mgmt-resource>=23.0.0
azure-mgmt-applicationinsights>=4.0.0
azure-mgmt-loganalytics>=13.0.0
azure-mgmt-subscription>=3.1.1
azure-monitor-opentelemetry==1.8.6
azure-monitor-query>=1.4.0
opentelemetry-instrumentation-openai-v2==2.3b0
openai==2.24.0

# Native crypto: last PyPI win_arm64 wheels (cp311-abi3 / cp38-abi3)
cryptography==46.0.3 ; platform_system == "Windows" and platform_machine == "ARM64"

# Evaluation and ML libraries (win_arm64 wheels from these floors)
pandas>=2.2.0
numpy>=2.3.0
scikit-learn>=1.8.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0

# Environment and Configuration
python-dotenv>=1.0.0

# API Integration
requests>=2.31.0
httpx>=0.25.0

# Logging and Monitoring
structlog>=23.1.0

# Data processing and visualization (win_arm64 wheels on 3.11+)
openpyxl>=3.1.0
matplotlib>=3.10.0
seaborn>=0.13.0

# Documentation and Reporting
markdown>=3.5.0
Jinja2>=3.1.0
